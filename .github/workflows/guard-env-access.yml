name: guard-env-access

on:
  pull_request:
  push:
    branches: [master]

jobs:
  guard-env-access:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run validate:env-access
      - run: npm run validate:env-example
